#include <stdio.h>
/**
 * main - A program that prints digit
 *
 * Return: 0(success)
 */
int main(void)
{
	int num;

	num = 0;

	while (num < 10)
	{
		printf("%d", n);

		num = num + 1;
	}
	return (0);
}
